import MigrationView from "@/app/_views/MigrationView";

export const metadata = {
  title: "Legacy Migration — Alliance OS",
  description: "AI-powered Excel import for migrating legacy insurance data",
};

export default function MigrationPage() {
  return <MigrationView />;
}
